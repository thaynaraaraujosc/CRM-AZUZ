# Copie para .env e preencha com a connection string real do banco (Railway > serviço MySQL > aba Variables).
DATABASE_URL="mysql://usuario:senha@host:porta/banco"

# WhatsApp não oficial via QR Code (worker Baileys separado, ver whatsapp-worker/README.md).
# BAILEYS_WORKER_URL = URL pública do serviço Railway do worker.
# WORKER_SECRET = mesmo valor cadastrado no worker.
BAILEYS_WORKER_URL="https://seu-worker.up.railway.app"
WORKER_SECRET="escolha-um-valor-aleatorio-longo"
